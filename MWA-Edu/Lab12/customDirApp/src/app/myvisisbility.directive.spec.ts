import { MyvisisbilityDirective } from './myvisisbility.directive';

describe('MyvisisbilityDirective', () => {
  it('should create an instance', () => {
    const directive = new MyvisisbilityDirective();
    expect(directive).toBeTruthy();
  });
});
